{% if typeTwig == 'php' %}
	{% set pathContent = 'templates/layout/connect/' %}
{% else %}
	{% set pathContent = '../' %}
{% endif %}

{% extends (typeTwig == 'php') ? 'templates/layout/connect/wrapper.twig' : 'wrapper.twig' %}

{% block wrapper_sections %}

	<section class="su-grid-1">
		<div class="su-grid-1_container">
			{% block content %}
			{% endblock %}
		</div>
	</section>

{% endblock %}
