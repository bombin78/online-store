{% if typeTwig == 'php' %}
	{% set pathPopupContainer = 'templates/layout/popup/' %}
{% endif %}

<div class="su-popup-container">
	{% if popupSearch %}
		{# Всплывающее окно строки поиска #}
		{% include pathPopupContainer~'blocks/popup-search.twig' %}
	{% endif %}
</div>
